# Raw source assets

This directory is intentionally ignored by Godot.

It contains vendor/source files that are kept for Blender cleanup, license verification, conversion, and future re-export. Runtime-ready `.glb` assets will live under `assets/runtime/` and are the only models Godot should import.
